import Nav from "../../components/Nav"; import catalog from "../../data/seed/catalog.json";
export default function Page(){const exams=catalog.exams as any[];return <><Nav/><main className="container"><h1>Acervo</h1><p className="muted">Catálogo baseado na página oficial. Status indica o andamento da importação.</p><div className="grid">{exams.map((e,i)=><div className="card" key={i}><b>{e.year} • {e.shift}{e.edition?"/"+e.edition:""}</b><p className="muted">{e.questions_expected?e.questions_expected+" questões esperadas":"Quantidade a identificar"}</p><small>Status: {e.status}</small></div>)}</div></main></>}