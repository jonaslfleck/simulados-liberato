"use client";import {useState} from "react";import {useRouter} from "next/navigation";import Nav from "../../components/Nav";export default function Page(){const r=useRouter();const [n,setN]=useState(10);return <><Nav/><main className="container"><h1>Simulado personalizado</h1><div className="card" style={{maxWidth:620}}><label>Quantidade de questões</label><select value={n} onChange={e=>setN(+e.target.value)} style={{display:"block",padding:12,width:"100%",margin:"8px 0 18px"}}>{[10,20,30,40].map(x=><option key={x}>{x}</option>)}</select><p><input type="checkbox" defaultChecked/> Português</p><p><input type="checkbox" defaultChecked/> Matemática</p><p><input type="checkbox" defaultChecked/> Usar questões reais importadas quando disponíveis</p><button className="btn" onClick={()=>r.push("/simulado/demo")}>Gerar {n} questões</button></div></main></>}