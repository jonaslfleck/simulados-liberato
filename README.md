# Simulados Liberato V9

Versão corrigida para Next.js/Vercel. Execute `npm install` e depois `npm run build` antes do deploy.

Variáveis: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SERVICE_ROLE_KEY.


## V10
Inclui `types/pdf-parse.d.ts` para corrigir o erro TypeScript de declaração ausente do `pdf-parse`.
